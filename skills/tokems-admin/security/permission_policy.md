# Permission Policy

The machine-readable approvals in `permission_policy.json` cover the connector's bounded network, local file write, credential-store subprocess, and browser authorization interaction surfaces. Review expires annually and whenever host policy, credential storage, action execution, or target adapters change.

Runtime permission metadata supplements TokEMS server authorization. It does not grant scopes, organization access, browser approval, or target-platform native enforcement.
