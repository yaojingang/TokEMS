# YYYY-MM-DD TokEMS 生产发布记录

> 发布状态：准备中 / 已发布 / 已回滚 / 已阻塞
>
> 执行时间：YYYY-MM-DD HH:mm，Asia/Shanghai
>
> 操作者：
>
> 长期规则：`docs/production-deployment-runbook.md`

## 1. 发布目标

- 本次用户可见变化：
- 本次代码和数据范围：
- 明确不进入本次发布的内容：

## 2. GitHub 和构建身份

| 项目                        | 本次值        |
| --------------------------- | ------------- |
| PR                          |               |
| CI                          |               |
| 镜像发布工作流              |               |
| 目标提交                    |               |
| 构建时间                    |               |
| 最高迁移                    |               |
| 迁移 SHA-256                |               |
| 发布分支                    | `origin/main` |
| Release descriptor digest   |               |
| Source Bundle SHA-256       |               |
| Descriptor verifier SHA-256 |               |
| 目标平台                    |               |

## 3. 发布前检查

- [ ] 服务器工作区干净
- [ ] `production` 跟踪 `origin/main`
- [ ] Compose 配置通过
- [ ] Nginx 配置通过
- [ ] 预构建模式的磁盘与备份容量满足要求；主机构建时额外通过 10 GiB 内存门禁
- [ ] 没有并行发布任务
- [ ] GitHub 必需检查成功

## 4. 备份和回滚点

| 项目                 | 本次值            |
| -------------------- | ----------------- |
| 备份目录             |                   |
| 数据库 dump          |                   |
| dump SHA-256         |                   |
| `pg_restore --list`  |                   |
| 镜像回滚标签         |                   |
| MinIO 或对象存储备份 | 不适用 / 备份标识 |
| 发布前版本           |                   |

## 5. 数据库和模板

- 是否包含新迁移：是 / 否
- `db-init` 结果：
- 是否执行规范模板同步：是 / 否
- 规范组织和大会：`geo-conference` / `tokems26`
- 规范快照检查：`pnpm canonical:check` 成功 / 失败
- 完整规范快照文件 SHA-256：
- 前台派生快照文件 SHA-256：
- 脱敏检查：管理员身份、API/第三方凭据、个人数据、交易数据、销量、审计记录均未进入快照
- 同步前报名数：
- 同步后报名数：
- 同步前订单数：
- 同步后订单数：
- 票种和配额销量校验：

## 6. 镜像和容器切换

| 服务              | 镜像摘要 | 最终状态 |
| ----------------- | -------- | -------- |
| API               |          |          |
| Worker            |          |          |
| Web / payment-web |          |          |
| Admin             |          |          |
| Gateway           |          |          |
| notification-sink |          |          |

六个服务均填写 descriptor 固定的 GHCR digest；容器验收同时记录实际 image ID。

## 7. 验证结果

- [ ] `docker compose ps` 长期服务全部 healthy
- [ ] `version.json.sha` 等于目标提交
- [ ] API `status=ok`
- [ ] `database.migration.ok=true`
- [ ] 服务器本机首页、后台、支付页可访问
- [ ] 公网首页、后台、支付页可访问
- [ ] 当前大会为 `tokems26`
- [ ] 前台模板、文案和大会设置符合发布目标
- [ ] 报名、订单、票、发票和库存数据符合预期

公网检查摘要：

| 入口     | HTTP 状态 | 版本或内容结果 |
| -------- | --------- | -------------- |
| 大会前台 |           |                |
| 运营后台 |           |                |
| 支付页面 |           |                |
| 版本接口 |           |                |
| 健康接口 |           |                |

## 8. 异常和处理

- 异常：
- 影响：
- 根因：
- 处理：
- 是否触发回滚：

## 9. 最终结论

- 最终状态：
- 线上提交：
- 线上迁移：
- 回滚点：
- 剩余风险：
- 下次发布前事项：

本记录不得包含 `.env` 全文、数据库连接、密码、密钥、令牌、私钥或用户隐私数据。
